name: 自动生成 Sing-box 规则

on:
  workflow_dispatch:
  push:
    branches:
      - main
  # schedule:
  #   - cron: '0 0 * * 1'

jobs:
  convert_and_commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write 

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            v2fly2singbox.py

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Clone domain-list-community data (Sparse Checkout)
        run: |
          git clone --depth 1 --filter=blob:none --sparse https://github.com/v2fly/domain-list-community.git
          cd domain-list-community
          git sparse-checkout set data/
          # [修正点1] 确保使用 master 分支 (v2fly 仓库主分支名为 master)
          git pull origin master
          cd ..

      - name: Run Converter Script
        run: python v2fly2singbox.py
        
      - name: Commit generated file (google.json)
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: '🤖 Auto: Update sing-box rule (google.json)'
          # [修正点2] 将 'files' 改为正确的参数名 'file_pattern'
          file_pattern: 'google.json'
